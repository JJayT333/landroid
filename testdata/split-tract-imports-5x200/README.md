# Split tract import files (5 x 200)

Split tract CSV imports are generated on demand by `scripts/large-deskmap-stress.js`.
They are intentionally not committed to keep repository fixture volume lean.
