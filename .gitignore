node_modules/
CHATGPTFAIL.pdf
.DS_Store
.claude/
TORS_Documents/
dist/
dist-node/
backend/*/lambda.zip
playwright-report/
test-results/

# Large AI-wizard binary fixtures (xlsx committed, zip/tif kept locally)
tests/fixtures/ai-wizard/**/*.zip
tests/fixtures/ai-wizard/**/*.tif
tests/fixtures/ai-wizard/**/*.tiff

# Local GIS source packages are large reference data, not repo artifacts.
/local-gis-source/
/*.ppkx
/*.ppkx*.zip

# Local worktrees + operator scratch artifacts (keep `git add -A` from sweeping
# them in). Committed map sample lives at public/samples/, not the repo root.
/.worktrees/
/JSON.json
/*.landroid
/All Tract Numbers.pdf
/All Tract Numbers.svg
/docs/landroid-deep-audit-prompt.md
